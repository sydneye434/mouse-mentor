# Developed by Sydney Edwards — Frontend (Vite)
# Copy to .env.local for local overrides. On Vercel, set in Project → Environment Variables.
#
# Backend API (no trailing slash). Required for production builds against Render.
# Leave unset in dev to use the Vite proxy to http://localhost:8000
# VITE_API_URL=https://your-mouse-mentor-api.onrender.com
